name: CI

on:
  push:
    branches:
      - '**'
  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:
  prepare:
    name: 'Prepare workflow'
    uses: eliashaeussler/gha/.github/workflows/preparation.yaml@0493ad446f3b35cfc9b7ed75330e91a6bd061728 # 1.1.7

  checks:
    name: 'Checks'

    needs: [prepare]
    if: ${{ needs.prepare.outputs.continue == 'true' }}

    permissions:
      contents: read
      security-events: write

    uses: eliashaeussler/gha/.github/workflows/checks.yaml@0493ad446f3b35cfc9b7ed75330e91a6bd061728 # 1.1.7
    with:
      npm: true

  devsecops:
    name: 'DevSecOps'

    needs: [prepare]
    if: ${{ needs.prepare.outputs.continue == 'true' }}

    permissions:
      contents: read

    secrets:
      DEVGUARD_TOKEN: ${{ secrets.DEVGUARD_TOKEN }}

    uses: eliashaeussler/gha/.github/workflows/devsecops.yaml@0493ad446f3b35cfc9b7ed75330e91a6bd061728 # 1.1.7
    with:
      devguard-asset-name: elias-haeussler/projects/typo3-badges/assets/typo3-badges

  tests:
    name: 'Tests'

    needs: [prepare]
    if: ${{ needs.prepare.outputs.continue == 'true' }}

    uses: eliashaeussler/gha/.github/workflows/composer-test-coverage.yaml@0493ad446f3b35cfc9b7ed75330e91a6bd061728 # 1.1.7
    with:
      build-command: 'build'
