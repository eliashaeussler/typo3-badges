name: CI

on:
  push:
    branches:
      - '**'
  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:
  prepare:
    name: 'Prepare workflow'
    uses: eliashaeussler/gha/.github/workflows/preparation.yaml@4d49615252d2d4710ef8ec80f45dc2cfa95071d5 # 0.9.4

  checks:
    name: 'Checks'

    needs: [prepare]
    if: ${{ needs.prepare.outputs.continue == 'true' }}

    permissions:
      contents: read
      security-events: write

    uses: eliashaeussler/gha/.github/workflows/checks.yaml@4d49615252d2d4710ef8ec80f45dc2cfa95071d5 # 0.9.4
    with:
      npm: true

  devsecops:
    name: 'DevSecOps'

    needs: [prepare]
    if: ${{ needs.prepare.outputs.continue == 'true' }}

    permissions:
      contents: read

    secrets:
      DEVGUARD_TOKEN: ${{ secrets.DEVGUARD_TOKEN }}

    uses: eliashaeussler/gha/.github/workflows/devsecops.yaml@4d49615252d2d4710ef8ec80f45dc2cfa95071d5 # 0.9.4
    with:
      devguard-asset-name: elias-haeussler/projects/typo3-badges/assets/typo3-badges

  tests:
    name: 'Tests'

    needs: [prepare]
    if: ${{ needs.prepare.outputs.continue == 'true' }}

    uses: eliashaeussler/gha/.github/workflows/composer-test-coverage.yaml@4d49615252d2d4710ef8ec80f45dc2cfa95071d5 # 0.9.4
    with:
      build-command: 'build'
