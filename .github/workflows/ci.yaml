name: CI

on:
  push:
    branches:
      - '**'
  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:
  prepare:
    name: 'Prepare workflow'
    uses: eliashaeussler/gha/.github/workflows/preparation.yaml@27331f61535ca3eca7e74928bb93e3a0b0007225 # 1.0.0

  checks:
    name: 'Checks'

    needs: [prepare]
    if: ${{ needs.prepare.outputs.continue == 'true' }}

    permissions:
      contents: read
      security-events: write

    uses: eliashaeussler/gha/.github/workflows/checks.yaml@27331f61535ca3eca7e74928bb93e3a0b0007225 # 1.0.0
    with:
      npm: true

  devsecops:
    name: 'DevSecOps'

    needs: [prepare]
    if: ${{ needs.prepare.outputs.continue == 'true' }}

    permissions:
      contents: read

    secrets:
      DEVGUARD_TOKEN: ${{ secrets.DEVGUARD_TOKEN }}

    uses: eliashaeussler/gha/.github/workflows/devsecops.yaml@27331f61535ca3eca7e74928bb93e3a0b0007225 # 1.0.0
    with:
      devguard-asset-name: elias-haeussler/projects/typo3-badges/assets/typo3-badges

  tests:
    name: 'Tests'

    needs: [prepare]
    if: ${{ needs.prepare.outputs.continue == 'true' }}

    uses: eliashaeussler/gha/.github/workflows/composer-test-coverage.yaml@27331f61535ca3eca7e74928bb93e3a0b0007225 # 1.0.0
    with:
      build-command: 'build'
