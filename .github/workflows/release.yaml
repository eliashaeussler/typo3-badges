name: Release

on:
  push:
    tags:
      - '*'
  workflow_dispatch:

jobs:
  release:
    name: 'Create release'

    if: ${{ startsWith(github.ref, 'refs/tags/') }}

    permissions:
      contents: write

    uses: eliashaeussler/gha/.github/workflows/github-release.yaml@c5d066ebd1c130e32d87bf7e7c8a1c3ca64bd14b # 0.8.2

  deploy-prod:
    name: 'Deploy to production'
    runs-on: ubuntu-latest

    if: ${{ startsWith(github.ref, 'refs/tags/') }}

    environment:
      name: production
      url: https://typo3-badges.dev

    concurrency:
      group: prod
      cancel-in-progress: true

    permissions:
      contents: read

    env:
      APP_ENV: prod

    steps:
      - name: 'Checkout'
        uses: eliashaeussler/gha/.github/actions/checkout@c5d066ebd1c130e32d87bf7e7c8a1c3ca64bd14b # 0.8.2

      - name: 'Setup PHP'
        uses: eliashaeussler/gha/.github/actions/setup-php@c5d066ebd1c130e32d87bf7e7c8a1c3ca64bd14b # 0.8.2
        with:
          php-version: 8.5

      - name: 'Install Composer packages'
        uses: eliashaeussler/gha/.github/actions/composer-install@c5d066ebd1c130e32d87bf7e7c8a1c3ca64bd14b # 0.8.2
        with:
          composer-options: '--no-dev --optimize-autoloader'

      - name: 'Build assets'
        uses: eliashaeussler/gha/.github/actions/build-assets@c5d066ebd1c130e32d87bf7e7c8a1c3ca64bd14b # 0.8.2

      - name: 'Deploy'
        uses: eliashaeussler/gha/.github/actions/deploy-app@c5d066ebd1c130e32d87bf7e7c8a1c3ca64bd14b # 0.8.2
        with:
          private-key: ${{ secrets.SSH_PRIVATE_KEY }}
          known-hosts: ${{ secrets.SSH_KNOWN_HOSTS }}
          deployer-version: '8.0.5'

  deploy-dev:
    name: 'Deploy to development'
    runs-on: ubuntu-latest

    if: ${{ github.event_name == 'workflow_dispatch' }}

    environment:
      name: production
      url: https://pre.typo3-badges.dev

    concurrency:
      group: dev
      cancel-in-progress: true

    permissions:
      contents: read

    env:
      APP_ENV: dev

    steps:
      - name: 'Checkout'
        uses: eliashaeussler/gha/.github/actions/checkout@c5d066ebd1c130e32d87bf7e7c8a1c3ca64bd14b # 0.8.2

      - name: 'Setup PHP'
        uses: eliashaeussler/gha/.github/actions/setup-php@c5d066ebd1c130e32d87bf7e7c8a1c3ca64bd14b # 0.8.2
        with:
          php-version: 8.5

      - name: 'Install Composer packages'
        uses: eliashaeussler/gha/.github/actions/composer-install@c5d066ebd1c130e32d87bf7e7c8a1c3ca64bd14b # 0.8.2
        with:
          composer-options: '--no-dev --optimize-autoloader'

      - name: 'Build assets'
        uses: eliashaeussler/gha/.github/actions/build-assets@c5d066ebd1c130e32d87bf7e7c8a1c3ca64bd14b # 0.8.2

      - name: 'Deploy'
        uses: eliashaeussler/gha/.github/actions/deploy-app@c5d066ebd1c130e32d87bf7e7c8a1c3ca64bd14b # 0.8.2
        with:
          command: dev
          private-key: ${{ secrets.SSH_PRIVATE_KEY }}
          known-hosts: ${{ secrets.SSH_KNOWN_HOSTS }}
          deployer-version: '8.0.5'
