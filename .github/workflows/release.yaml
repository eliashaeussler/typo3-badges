name: Release

on:
  push:
    tags:
      - '*'
  workflow_dispatch:

jobs:
  release:
    name: 'Create release'

    if: ${{ startsWith(github.ref, 'refs/tags/') }}

    permissions:
      contents: write

    uses: eliashaeussler/gha/.github/workflows/github-release.yaml@cd9d7395b05159994d09371388e4f804fafd82c2 # 1.0.1

  deploy-prod:
    name: 'Deploy to production'
    runs-on: ubuntu-latest

    if: ${{ startsWith(github.ref, 'refs/tags/') }}

    environment:
      name: production
      url: https://typo3-badges.dev

    concurrency:
      group: prod
      cancel-in-progress: true

    permissions:
      contents: read

    env:
      APP_ENV: prod

    steps:
      - name: 'Checkout'
        uses: eliashaeussler/gha/.github/actions/checkout@cd9d7395b05159994d09371388e4f804fafd82c2 # 1.0.1

      - name: 'Setup PHP'
        uses: eliashaeussler/gha/.github/actions/setup-php@cd9d7395b05159994d09371388e4f804fafd82c2 # 1.0.1
        with:
          php-version: 8.5

      - name: 'Install Composer packages'
        uses: eliashaeussler/gha/.github/actions/composer-install@cd9d7395b05159994d09371388e4f804fafd82c2 # 1.0.1
        with:
          composer-options: '--no-dev --optimize-autoloader'

      - name: 'Build assets'
        uses: eliashaeussler/gha/.github/actions/build-assets@cd9d7395b05159994d09371388e4f804fafd82c2 # 1.0.1

      - name: 'Deploy'
        uses: eliashaeussler/gha/.github/actions/deploy-app@cd9d7395b05159994d09371388e4f804fafd82c2 # 1.0.1
        with:
          private-key: ${{ secrets.SSH_PRIVATE_KEY }}
          known-hosts: ${{ secrets.SSH_KNOWN_HOSTS }}
          deployer-version: '8.0.5'

  deploy-dev:
    name: 'Deploy to development'
    runs-on: ubuntu-latest

    if: ${{ github.event_name == 'workflow_dispatch' }}

    environment:
      name: production
      url: https://pre.typo3-badges.dev

    concurrency:
      group: dev
      cancel-in-progress: true

    permissions:
      contents: read

    env:
      APP_ENV: dev

    steps:
      - name: 'Checkout'
        uses: eliashaeussler/gha/.github/actions/checkout@cd9d7395b05159994d09371388e4f804fafd82c2 # 1.0.1

      - name: 'Setup PHP'
        uses: eliashaeussler/gha/.github/actions/setup-php@cd9d7395b05159994d09371388e4f804fafd82c2 # 1.0.1
        with:
          php-version: 8.5

      - name: 'Install Composer packages'
        uses: eliashaeussler/gha/.github/actions/composer-install@cd9d7395b05159994d09371388e4f804fafd82c2 # 1.0.1
        with:
          composer-options: '--no-dev --optimize-autoloader'

      - name: 'Build assets'
        uses: eliashaeussler/gha/.github/actions/build-assets@cd9d7395b05159994d09371388e4f804fafd82c2 # 1.0.1

      - name: 'Deploy'
        uses: eliashaeussler/gha/.github/actions/deploy-app@cd9d7395b05159994d09371388e4f804fafd82c2 # 1.0.1
        with:
          command: dev
          private-key: ${{ secrets.SSH_PRIVATE_KEY }}
          known-hosts: ${{ secrets.SSH_KNOWN_HOSTS }}
          deployer-version: '8.0.5'
