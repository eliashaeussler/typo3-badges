name: Release

on:
  push:
    tags:
      - '*'
  workflow_dispatch:

jobs:
  release:
    name: 'Create release'

    if: ${{ startsWith(github.ref, 'refs/tags/') }}

    permissions:
      contents: write

    uses: eliashaeussler/gha/.github/workflows/github-release.yaml@1fb632290bce95f37a13857172e1ac6bf1a821c1 # 0.8.3

  deploy-prod:
    name: 'Deploy to production'
    runs-on: ubuntu-latest

    if: ${{ startsWith(github.ref, 'refs/tags/') }}

    environment:
      name: production
      url: https://typo3-badges.dev

    concurrency:
      group: prod
      cancel-in-progress: true

    permissions:
      contents: read

    env:
      APP_ENV: prod

    steps:
      - name: 'Checkout'
        uses: eliashaeussler/gha/.github/actions/checkout@1fb632290bce95f37a13857172e1ac6bf1a821c1 # 0.8.3

      - name: 'Setup PHP'
        uses: eliashaeussler/gha/.github/actions/setup-php@1fb632290bce95f37a13857172e1ac6bf1a821c1 # 0.8.3
        with:
          php-version: 8.5

      - name: 'Install Composer packages'
        uses: eliashaeussler/gha/.github/actions/composer-install@1fb632290bce95f37a13857172e1ac6bf1a821c1 # 0.8.3
        with:
          composer-options: '--no-dev --optimize-autoloader'

      - name: 'Build assets'
        uses: eliashaeussler/gha/.github/actions/build-assets@1fb632290bce95f37a13857172e1ac6bf1a821c1 # 0.8.3

      - name: 'Deploy'
        uses: eliashaeussler/gha/.github/actions/deploy-app@1fb632290bce95f37a13857172e1ac6bf1a821c1 # 0.8.3
        with:
          private-key: ${{ secrets.SSH_PRIVATE_KEY }}
          known-hosts: ${{ secrets.SSH_KNOWN_HOSTS }}
          deployer-version: '8.0.5'

  deploy-dev:
    name: 'Deploy to development'
    runs-on: ubuntu-latest

    if: ${{ github.event_name == 'workflow_dispatch' }}

    environment:
      name: production
      url: https://pre.typo3-badges.dev

    concurrency:
      group: dev
      cancel-in-progress: true

    permissions:
      contents: read

    env:
      APP_ENV: dev

    steps:
      - name: 'Checkout'
        uses: eliashaeussler/gha/.github/actions/checkout@1fb632290bce95f37a13857172e1ac6bf1a821c1 # 0.8.3

      - name: 'Setup PHP'
        uses: eliashaeussler/gha/.github/actions/setup-php@1fb632290bce95f37a13857172e1ac6bf1a821c1 # 0.8.3
        with:
          php-version: 8.5

      - name: 'Install Composer packages'
        uses: eliashaeussler/gha/.github/actions/composer-install@1fb632290bce95f37a13857172e1ac6bf1a821c1 # 0.8.3
        with:
          composer-options: '--no-dev --optimize-autoloader'

      - name: 'Build assets'
        uses: eliashaeussler/gha/.github/actions/build-assets@1fb632290bce95f37a13857172e1ac6bf1a821c1 # 0.8.3

      - name: 'Deploy'
        uses: eliashaeussler/gha/.github/actions/deploy-app@1fb632290bce95f37a13857172e1ac6bf1a821c1 # 0.8.3
        with:
          command: dev
          private-key: ${{ secrets.SSH_PRIVATE_KEY }}
          known-hosts: ${{ secrets.SSH_KNOWN_HOSTS }}
          deployer-version: '8.0.5'
