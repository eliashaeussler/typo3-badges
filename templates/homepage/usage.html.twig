<div class="flex items-center justify-space-between">
    <h2 class="font-bold text-xl flex-grow">&#x1F680;&nbsp;Usage</h2>
</div>
<hr class="my-2 border-gray-200">

<p class="text-gray mt-3 md:mt-4">
    This API provides endpoints for
    {% for provider in providers %}
        <a href="{{ provider.providerUrl }}" class="font-bold hover:underline">{{ provider.name }}</a>{% if loop.revindex0 == 1 %} and {% else %},{% endif %}
    {% endfor %}
    allowing badges to be rendered.
</p>
<p class="text-gray mt-4">
    Each endpoint supports the
    <code class="bg-gray-700 rounded-md text-white text-sm px-1.5 py-0.5">svg</code>
    format with the rendered SVG badge and the
    <code class="bg-gray-700 rounded-md text-white text-sm px-1.5 py-0.5">json</code>
    format with JSON data for external badge providers.
</p>
